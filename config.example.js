var channels = {
    FOO: "#foo",
    BAR: "#bar",
    GEN: "#gen"
}

var config = {
  channels: [channels.FOO], // Channels you want to join 
  server: "irc.foo.example.com", // IRC server
  botName: "botNameHere", // Give your bot a name.
  defaultMembers: ["Alice", "Bob", "Jamie"] // Nicks of default lunch group members.
};
